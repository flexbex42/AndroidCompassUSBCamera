<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme: use MaterialComponents so Material widgets are available -->
    <style name="Base.Theme.Camera6" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Color tokens used by Material components; customize to your branding -->
        <item name="colorPrimary">@color/md_primary</item>
        <item name="colorOnPrimary">@color/md_on_primary</item>
        <item name="colorSecondary">@color/md_secondary</item>
        <item name="colorOnSecondary">@color/md_on_secondary</item>
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@color/md_on_surface</item>

        <!-- Do not set materialToolbarStyle here — some library versions don't expose this attr.
             Instead set the toolbar style explicitly in the layout. -->
    </style>

    <!-- Keep your existing alias theme name so code/manifest referencing Theme.Camera6 continues to work -->
    <style name="Theme.Camera6" parent="Base.Theme.Camera6" />

    <style name="Camera2TopButtonStyle">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:padding">8dp</item>
    </style>
</resources>